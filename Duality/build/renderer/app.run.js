/// <reference path="../../typings/tsd.d.ts" />
var app;
(function (app) {
    'use strict';
    var RunBlock = (function () {
        function RunBlock($log) {
        }
        return RunBlock;
    }());
    app.RunBlock = RunBlock;
})(app || (app = {}));
